<Page x:Class="Bilet8.Add"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Bilet8"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      Title="Add">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Foreground="Black" FontFamily="Times New Roman" Text="Список спектаклей" FontSize="35" VerticalAlignment="Center" 
                           HorizontalAlignment="Center" Margin="10"/>
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1">
            <ListView x:Name="perfomanceListView"
                  Grid.Row="1" 
                  ScrollViewer.VerticalScrollBarVisibility="Visible"
                  ScrollViewer.CanContentScroll="True"
                  SelectionMode="Single"
                  Grid.Column="0" Height="550">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Viewbox
                        Height="60" MouseLeftButtonDown="Viewbox_MouseLeftButtonDown">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBlock x:Name="idPerfomance"
                                       Grid.Row="0"
                                       FontSize="40"
                                       FontFamily="Times New Roman"
                                       Margin="0 20 0 0"
                                       Text="{Binding idPerfomance}"/>
                                <TextBlock x:Name="namePerfomance"
                                       Grid.Row="1"
                                       FontSize="40"
                                       FontFamily="Times New Roman"
                                       Margin="0 20 0 0"
                                       Text="{Binding namePerfomance}"/>
                                <TextBlock x:Name="datePerfomance"
                                       Grid.Row="2"
                                       FontFamily="Times New Roman"
                                       FontSize="40"
                                       Margin="0 20 0 0"
                                       Text="{Binding datePerfomance}"/>

                            </Grid>
                        </Viewbox>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="2">
            <Button Content="Добавить спектакль" Click="Button_Click" Margin="0,0,20,0"/>
            <Button Content="Добавить билет" Click="Button_Click_1"/>
        </StackPanel>
    </Grid>
</Page>
